# Agents

## NEVER (hard rules — breaking these is a failure)
- NEVER commit, push, or create PRs without first asking the user and getting explicit approval. Always show git status/diff first and wait for confirmation.
